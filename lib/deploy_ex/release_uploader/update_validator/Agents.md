# Agents

## Purpose
Parser utilities for deps tree and mix.lock diffs.

## Working agreements
- Keep parsers pure and deterministic.
- Ensure git and mix output parsing stays compatible with Mix defaults.
- Add fixtures and tests when parsing rules change.
