# Agents

## Purpose
Configuration defaults and runtime settings for deploy_ex.

## Working agreements
- Keep all config under the :deploy_ex application key.
- Avoid runtime Mix.env usage; defaults should live in DeployEx.Config.
- Document new config options in README.
