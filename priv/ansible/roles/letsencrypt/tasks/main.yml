# Cert issuance — DNS-challenge path (prod + domain-backed QA nodes). Issues
# 90-day certs scoped to {{ domain }}.
- name: letsencrypt (DNS-01 challenge — domain-backed nodes)
  block:
    - name: Ensure Cloudflare API token is configured
      fail:
        msg: "cloudflare_api_token is required for certbot DNS challenge (Zone:DNS:Edit)."
      when: cloudflare_api_token is not defined or cloudflare_api_token | length == 0

    - name: Install required packages
      apt:
        name:
          - certbot
          - python3-certbot-dns-cloudflare
        state: present
        update_cache: yes

    - name: Create Cloudflare credentials file for certbot
      template:
        src: cloudflare-credentials.ini.j2
        dest: /etc/letsencrypt/cloudflare-credentials.ini
        mode: '0600'
        owner: root
        group: root
      when: cloudflare_api_token is defined

    - name: Ensure certificates exist or generate them
      shell: |
        #!/bin/bash
        set -euo pipefail

        DOMAIN="{{ domain }}"
        EMAIL="{{ letsencrypt_email }}"
        CREDS_FILE="/etc/letsencrypt/cloudflare-credentials.ini"

        # Check if Cloudflare credentials exist
        if [ ! -f "$CREDS_FILE" ]; then
            echo "Cloudflare credentials not found at $CREDS_FILE"
            exit 1
        fi

        # Obtain the initial certificate using the DNS challenge. Ongoing renewal
        # is handled by the cert-renewal timer + cert-initial boot service below.
        echo "Obtaining certificate for $DOMAIN using DNS challenge..."
        certbot certonly \
            --dns-cloudflare \
            --dns-cloudflare-credentials "$CREDS_FILE" \
            -d "$DOMAIN" \
            --non-interactive \
            --agree-tos \
            --email "$EMAIL"

        echo "Certificate obtained successfully"
      args:
        creates: "/etc/letsencrypt/live/{{ domain }}/fullchain.pem"
  when: not letsencrypt_use_public_ip | bool
  become: true

# Cert issuance — IP-cert path, used by standalone QA nodes (set via
# --public-ip-cert on qa.create). Gets a real Let's Encrypt cert scoped to the
# node's public IPv4 using the short-lived profile (~7-day cert lifetime).
# Requires certbot ≥ 4.0, installed via snap since Debian/Ubuntu apt packages
# are too old.
- name: letsencrypt (Let's Encrypt HTTP-01 for public IP — standalone QA nodes)
  block:
    - name: Install snapd
      apt:
        name: snapd
        state: present
        update_cache: yes

    # snapd's first boot seeds the system store, and `snap install` calls
    # api.snapcraft.io/v2/snaps/refresh. If seeding is still in flight, or
    # the refresh call hits a transient network blip, snapd returns
    # "context canceled". Block until seeding completes and retry the
    # install a few times with backoff so transient API failures recover.
    - name: Wait for snapd to finish seeding
      command: snap wait system seed.loaded
      changed_when: false

    - name: Install certbot via snap (needs ≥ 4.0 for --preferred-profile shortlived)
      command: snap install --classic certbot
      args:
        creates: /snap/bin/certbot
      register: snap_install_certbot
      retries: 5
      delay: 30
      until: snap_install_certbot is succeeded

    - name: Symlink certbot into /usr/bin
      file:
        src: /snap/bin/certbot
        dest: /usr/bin/certbot
        state: link
        force: true

    # ansible_default_ipv4 reflects the EC2 primary NIC (private 10.x); LE won't
    # issue against that. Pull the public IPv4 from IMDSv2 instead. Token TTL is
    # the call-site lifetime — long enough for the cert request, short enough to
    # not linger.
    - name: Get IMDSv2 token
      uri:
        url: http://169.254.169.254/latest/api/token
        method: PUT
        headers:
          X-aws-ec2-metadata-token-ttl-seconds: "300"
        return_content: true
      register: imds_token

    - name: Read public IPv4 from EC2 instance metadata
      uri:
        url: http://169.254.169.254/latest/meta-data/public-ipv4
        headers:
          X-aws-ec2-metadata-token: "{{ imds_token.content }}"
        return_content: true
      register: imds_public_ipv4

    - name: Bind node_public_ip to the IMDSv2 result
      set_fact:
        node_public_ip: "{{ imds_public_ipv4.content }}"

    # certbot's -d flag goes through DNSName() which rejects IPs client-side
    # ("will not issue certificates for a bare IP address") even though LE's
    # shortlived profile DOES support IPs. --ip-address is the correct flag for
    # IP SANs and bypasses the DNS sanity check.
    #
    # --required-profile (vs --preferred-profile) forces the request to fail if
    # the CA can't honor shortlived, which is the only profile that issues for
    # IPs today.
    #
    # --pre-hook / --post-hook stop + start nginx around issuance so certbot can
    # bind :80. They get persisted in the cert's renewal config, so the
    # cert-renewal timer below (and snap's own snap.certbot.renew.timer) cycle
    # nginx on their own during renewals.
    - name: Obtain Let's Encrypt short-lived cert for public IP
      shell: |
        set -euo pipefail
        certbot certonly \
          --standalone \
          --required-profile shortlived \
          --ip-address "{{ node_public_ip }}" \
          --non-interactive \
          --agree-tos \
          --email "{{ letsencrypt_email }}" \
          --pre-hook "systemctl stop nginx 2>/dev/null || true" \
          --post-hook "systemctl start nginx 2>/dev/null || true"
      args:
        creates: "/etc/letsencrypt/live/{{ node_public_ip }}/fullchain.pem"
  when: letsencrypt_use_public_ip | bool
  become: true

# Shared renewal scaffolding — installed on BOTH paths. The public-IP QA path
# previously had no managed renewal at all (no boot service, no frequent timer),
# so a QA node stopped past its ~7-day cert came back serving an expired cert
# (ERR_CERT_DATE_INVALID). The renewal cadence is set by letsencrypt_renewal_*
# defaults: QA (public-IP) checks hourly, prod (DNS) daily.
- name: letsencrypt (renewal timer + boot check — shared)
  block:
    - name: Create certificate renewal script
      template:
        src: renew-certificates.sh.j2
        dest: /usr/local/bin/renew-certificates.sh
        mode: '0755'
      notify: restart certificate renewal service

    - name: Create systemd service for certificate renewal
      template:
        src: cert-renewal.service.j2
        dest: /etc/systemd/system/cert-renewal.service
        mode: '0644'
      notify:
        - reload systemd
        - enable certificate renewal service

    - name: Create systemd timer for certificate renewal
      template:
        src: cert-renewal.timer.j2
        dest: /etc/systemd/system/cert-renewal.timer
        mode: '0644'
      notify:
        - reload systemd
        - enable certificate renewal timer

    - name: Create startup script for certificate check
      template:
        src: check-certificates.sh.j2
        dest: /usr/local/bin/check-certificates.sh
        mode: '0755'

    - name: Create systemd service for initial certificate check on boot
      template:
        src: cert-initial.service.j2
        dest: /etc/systemd/system/cert-initial.service
        mode: '0644'
      notify:
        - reload systemd
        - enable certificate initial service

    - name: Add certificate check to cloud-init (if it exists)
      lineinfile:
        path: /etc/rc.local
        line: '/usr/local/bin/check-certificates.sh'
        insertbefore: '^exit 0'
        mode: '0755'
        create: yes

    # Apply the new units/scripts before kicking off the boot check below.
    - name: Apply pending systemd unit changes
      meta: flush_handlers

    - name: Run initial certificate check now
      systemd:
        name: cert-initial
        state: started

    - name: Set proper permissions for certificate files
      file:
        path: /etc/letsencrypt
        state: directory
        recurse: yes
        mode: '0755'
        owner: root
        group: root
  become: true
